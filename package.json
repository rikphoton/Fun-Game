{
  "name": "neon-pulse-arcade",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "desktop": "vite build && npx electron .",
    "share": "npx localtunnel --port 3000"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
